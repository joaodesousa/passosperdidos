version: '3.8'

services:
  web:
    build: .
    container_name: django_api
    restart: always
    env_file: .env
    command: gunicorn api.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - supabase
